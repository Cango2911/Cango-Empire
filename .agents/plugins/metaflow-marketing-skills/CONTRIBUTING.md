# Contributing

1. Edit markdown under `skills/<slug>/` (start with `SKILL.md`, then `references/` as needed).
2. Run `npm run validate` before committing.

Do not commit secrets, API keys, or customer data.
